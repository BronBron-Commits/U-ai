[package]
name = "uai"
version = "0.1.0"
edition = "2021"

[dependencies]
# entropy fetch
ureq = "2.12"
rand = "0.8"
rand_chacha = "0.3"

# your existing dependencies
# (add more back here later if needed)
